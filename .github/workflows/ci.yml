name: CI

on:
  push:
    branches: ['**']
  pull_request:

permissions:
  contents: read

jobs:
  go:
    uses: ./.github/workflows/_ci.yml
    permissions:
      contents: read
      id-token: write
